import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Project141w85Component } from './project141w85.component';

describe('Project141w85Component', () => {
  let component: Project141w85Component;
  let fixture: ComponentFixture<Project141w85Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Project141w85Component]
    });
    fixture = TestBed.createComponent(Project141w85Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
